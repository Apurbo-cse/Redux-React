// State
const initialCounterState = {
  count: 0,
};
const initialUserState = {
  users: [
    {
      name: "Aputbo Roy",
    },
  ],
};

// Action   - Object -type,payload

// Increment Counter
{
  type: "INCREMENT";
}

// Decrement Counter
